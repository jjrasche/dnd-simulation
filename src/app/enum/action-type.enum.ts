export enum ActionType {
    Attack,
    Cast,
    Dash,
    Disengage,
    Dodge,
    Help,
    Hide,
    Ready,
    Search,
    Use
}

/**
 * describes 
 */
export enum ActionTypeEnum {
    Throw = "Throw",
    Hit = "Hit",
    Shoot = "Shoot",
}
