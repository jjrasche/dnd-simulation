export enum ActionType {
    Attack,
    Cast,
    Dash,
    Disengage,
    Dodge,
    Help,
    Hide,
    Ready,
    Search,
    Use
}

export enum ActionTypeEnum {
    Throw = "Throw",
    Hit = "Hit",
    Shoot = "Shoot",
}
